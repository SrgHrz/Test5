# On top of being able to generate a random integer
# one can also use the random() function to generate a
# random float r such that: 0 <= r < 1.0

import random
number1 = random.random()
print(number1)
