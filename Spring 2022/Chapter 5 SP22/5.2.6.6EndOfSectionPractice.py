# Show the error in the following code
print("c")

count = 0
while count < 100:
    print(count)
    count += 1
# Originally this program was right. It was not
# an infinite loop because it did terminate
# It's just that we couldn't see the values
# the only thing that was added was "print(count)"
# to be able to view the numbers