# We will compute the area of a circle using a constant

# Enter radius
radius = eval(input("Enter a value for the radius: "))

# Define constant
PI = 3.14159

# Area formula
area = PI * radius * radius

# Print result
print("The area of a circle of radius", radius, "is", area)