import math
# Prompt user to enter three points
x1, y1, x2, y2, x3, y3 = eval(input("Enter six values of three points separated by commas: "))

# define formula for magnitudes for each point
# define formula for angle of each corner