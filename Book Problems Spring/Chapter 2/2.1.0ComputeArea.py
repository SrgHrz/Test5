# Assign a value to the radius
radius = 20 # The radius is now 20
# compute area
area = radius * radius * 3.14159
# Display results
print("That area for a circle of radius", radius, "is", area)

# Now this will be done with the use of a constant
# Assign a radius
radius = 20
PI = 3.14159 # This will be our constant
area = radius * radius * PI
print("The area of a circle of radius", radius, " is", area)

