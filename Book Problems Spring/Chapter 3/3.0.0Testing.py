# This will show how simple built in functions work
abs(-3) # returns the absolute value
abs(-3.5)
max(1,2,3,4,5) # This returns the max number of the list
min(1,2,3,4,5) # This returns the min number of the group
print("Hello")